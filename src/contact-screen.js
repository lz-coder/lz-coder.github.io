const contact_dialog = document.getElementById("contact_dialog");

const showContactDialog = () => {
    contact_dialog.showModal();
}

const closeContactDialog = () => {
    contact_dialog.close();
}

